# B6-2: 내가 고친 코드 설명을 AI가 대신 써주는 도우미 만들기

## 📋 과제 정보

| 항목 | 내용 |
|------|------|
| **과목** | 클라우드와 AI API (Cloud & AI API) |
| **난이도** | ★☆☆ (Lv.1) |
| **학습 시간** | 40분 |
| **필수 여부** | ✅ 필수 |
| **진행 상태** | 평가전 |
| **과제 번호** | 185016 |

---

## 🎯 미션 소개

개발자가 코드를 고치고 커밋할 때, "이 변경이 뭘 하는지" 설명하는 커밋 메시지와 PR 설명을 작성하는 건 번거로운 일이다. 이 미션에서는 **AI API를 활용해서 Git 변경 사항을 분석하고, 커밋 메시지와 PR 초안을 자동으로 생성**하는 CLI 도구를 만든다.

`git diff`를 수집해서 AI에게 전달하면, AI가 변경 내용을 분석해서 적절한 형식의 커밋 메시지나 PR 본문(Why/What/How to Test)을 작성해준다. 단, 민감정보가 AI에게 전달되지 않도록 마스킹 처리하는 보안 기능도 함께 구현한다.

![미션 설명 이미지](mission.jpg)

---

## 🎓 학습 목표

이 과제를 완료한 뒤, 다음을 설명할 수 있어야 한다:

1. **AI API 연동** — 환경변수 기반 API Key 관리, 로컬 LLM(Ollama/라마) 연결 방법
2. **Git 데이터 수집** — `git status`, `git diff`로 변경 사항을 프로그램에서 읽는 방법
3. **프롬프트 설계** — diff를 AI가 이해할 수 있는 컨텍스트로 변환하는 방법
4. **보안 처리** — diff에 포함된 민감정보를 마스킹하여 프롬프트에 포함시키지 않는 방법
5. **CLI 설계** — argparse로 명령/옵션을 처리하고 단일 실행 흐름을 설계하는 방법

---

## 📦 최종 결과물

1. **GitHub 저장소 URL** — 소스 코드 (.env 제외, README 포함)
2. **실행 결과** — 터미널 출력 (커밋 메시지 자동 생성 + PR 초안 자동 생성)

---

## ✅ 기능 요구사항

### 명령 (2개)
| 명령 | 기능 | API 호출 |
|------|------|----------|
| `python main.py commit` | 커밋 메시지 자동 생성 | 1회 |
| `python main.py pr` | PR 제목/본문 자동 생성 | 1회 |

### 실행 흐름
```
1. git status 수집 → 변경 파일 목록
2. git diff 수집 → 실제 변경 내용
3. 민감정보 마스킹 (항상 수행)
4. 프롬프트 조립 (diff → AI 입력 컨텍스트)
5. AI API 호출 (1회)
6. 결과 출력 (커밋 메시지 또는 PR 초안)
```

### 에러/예외 처리 (필수)
| 상황 | 출력 |
|------|------|
| API Key 미설정 | `[ERROR] AI_API_KEY 환경변수가 설정되지 않았습니다.` + 설정 예시 |
| 변경사항 없음 | `[INFO] 변경 사항이 없습니다. 커밋 메시지를 생성하지 않고 종료합니다.` |
| API 호출 실패 | `[ERROR] AI API 오류: ...` (타임아웃/연결실패 등) |

### 커밋 메시지 출력 형식
```css
[INFO] Git status 수집 완료: 3개 파일 변경 감지
[INFO] Git diff 수집 완료: 128줄
[INFO] AI API 요청 중...
[DONE] 커밋 메시지 생성 완료

--- Commit Message ---
feat: Git 변경 사항 기반 커밋 메시지 자동 생성 기능 추가

- git diff 결과를 수집해 AI 입력 컨텍스트로 전달
- 커밋 메시지 템플릿(feat/fix 등) 생성 규칙 적용
----------------------
```

### PR 초안 출력 형식
```css
[INFO] 현재 브랜치: feature/commit-pr-generator
[INFO] AI API 요청 중...
[DONE] PR 초안 생성 완료

--- PR Title ---
feat: 커밋/PR 자동 생성 기능 추가

--- PR Body ---
## Why
- 팀 협업 시 커밋 메시지와 PR 설명 작성에 시간이 소요되어 자동 생성 도구가 필요했습니다.

## What
- git status, git diff 결과를 수집해 AI 입력 컨텍스트로 전달
- 커밋 메시지 자동 생성 기능 구현
- PR 제목/본문 자동 생성 기능 구현

## How to Test
- 환경변수 설정: export AI_API_KEY="YOUR_KEY"
- 커밋 메시지 생성: python main.py commit
- PR 초안 생성: python main.py pr
----------------
```

---

## 🛠️ 개발 환경

- Python 3.10 이상
- 웹 화면 없이, 터미널에서 실행되는 프로그램으로 구현한다
- AI API: NVIDIA NIM (build.nvidia.com, 무료 API Key)

---

## ⚠️ 제약 사항

### AI API 사용 범위
- AI API Key는 **환경변수로만 관리**, 코드에 하드코딩하지 않는다
- 과도한 비용 발생 방지를 위해, 1회 실행 시 요청 횟수는 **1~2회 이내** 제한 권장
- (권장) `commit`/`pr` 명령은 각각 AI API 1회 호출로 결과를 생성하고, 로그에 호출 횟수를 출력

### Git 연동 범위
- Git 변경 사항 수집은 **`git status`, `git diff` 범위로 제한**
- 커밋/PR 생성은 **초안 텍스트 출력까지**를 목표로 한다
- `git push`, GitHub PR 생성(API 연동) 등 **원격 저장소 자동 반영 기능은 구현하지 않는다**

### 보안/민감정보
- `git diff`에 포함될 수 있는 민감정보(API Key, 개인정보 등)는 **프롬프트에 포함하지 않도록 주의**
- 아래 중 **1개 이상**을 `-safe-mode` 같은 옵션으로 제공:
    - **(A)** diff에서 특정 패턴(API Key 형태, 이메일 등)을 **마스킹** 후 전송
    - **(B)** diff **일부만 전송** (예: 최대 10개 파일, 최대 200줄)
- 생성된 커밋/PR 문구는 **최종 정답이 아니며**, 사용자가 검토 후 적용한다

---

## 📝 결과 예시

아래는 정답이 아니라 참고 예시다. 실제 문구는 얼마든지 달라도 된다.

### 커밋 메시지 자동 생성
```css
python main.py commit
```
```
[INFO] Git status 수집 완료: 3개 파일 변경 감지
[INFO] Git diff 수집 완료: 128줄
[INFO] AI API 요청 중...
[DONE] 커밋 메시지 생성 완료

--- Commit Message ---
feat: Git 변경 사항 기반 커밋 메시지 자동 생성 기능 추가
----------------------
```

### PR 제목/본문 자동 생성
```css
python main.py pr
```
```
[INFO] 현재 브랜치: feature/commit-pr-generator
[INFO] AI API 요청 중...
[DONE] PR 초안 생성 완료

--- PR Title ---
feat: 커밋/PR 자동 생성 기능 추가

--- PR Body ---
## Why / ## What / ## How to Test
----------------
```

### API Key 미설정
```css
python main.py commit
```
```
[ERROR] AI_API_KEY 환경변수가 설정되지 않았습니다.
  예) export AI_API_KEY="YOUR_KEY"
```

### 변경 사항이 없는 경우
```css
python main.py commit
```
```
[INFO] 변경 사항이 없습니다. 커밋 메시지를 생성하지 않고 종료합니다.
```

---

## 📊 평가 정보

- 평가 대상: 예

---

> *이 문서는 Codyssey AI/SW 기초 과정의 과제 내용을 기반으로 작성되었습니다.*
